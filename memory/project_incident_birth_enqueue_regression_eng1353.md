---
name: project_incident_birth_enqueue_regression_eng1353
description: "Fleet-wide conversation-kernel birth-enqueue regression (ENG-1353 / commit 821d29c6) since 2026-07-24 — new connect/gtkm loops stuck \"reading\" or blank"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6b08e346-edab-4c9d-83ca-3c77d5d7c16e
---

**INCIDENT (diagnosed 2026-07-27, prod).** New "Get to know me" (blank screen) and "Get to know an &ee" / connect loops (perpetual "Reading…") on TestFlight. NOT a client bug and NOT a dead runtime — a fleet-wide **conversation-kernel birth-enqueue regression**.

**EXACT ROOT CAUSE (pinned): the outcome-contract gate added by commit `553c2048` ("feat(loops): add durable outcome receipts and run lab"), at `packages/db/src/queries/agent-runtimes.ts:3696-3704`** in `createRuntimeChannelMessage`: `if (!row.skillId || !row.skillVersion || !row.pinnedSkillDigest || !row.skillDigest || row.pinnedSkillDigest !== row.skillDigest) return { ok:false, reason:'outcome_contract_not_found' }` (line 3703). A birth passes `loopId` (conversations.ts:1316 `settleConversationBirthDebt`), so it enters this loopSnapshot block. **EVERY gtkm + connect loop in prod has `loops.skill_version = NULL` and `loops.skill_content_digest = NULL`** (loop_create never pins skill metadata for these skills — confirmed on loops created TODAY too), so the guard trips on `!row.skillVersion` → `outcome_contract_not_found` → `settleConversationBirthDebt` maps `!ok`→`debt_retained` → dispatcher backs off (only stale_epoch/conversation_mismatch skip backoff) → `conversations.birth_attempts` climbs to 60+, never discharges. **Andee turns work because agent-chat conversations carry NO loopId (26/26 null) → skip the gate entirely** (587 andee runs/day complete); connect-subject (48/48) + gtkm-read (23/23) all carry loopId → all hit it. Deployed alongside `821d29c6` (ENG-1353, migration `0145_gtkm_run_outcome_v1_14_0` re-seeded gtkm skill v1.14.0 — RED HERRING for the enqueue failure, related to [[project_gtkm_seed_drift_db_test_red]]). Deploy window bounded by: last successful birth `runtime_channel_messages.trigger_kind='birth'` at 2026-07-24 20:20:38; first stuck conversation 2026-07-25 04:04. **Zero births enqueued fleet-wide since.** (NOTE: the `runId: mintRunId()` removal in 821d29c6 is NOT the cause — run creation moved into createRuntimeChannelMessage's `createLoopRun`; that path is fine.)

**Mechanism (both symptoms = same break):** POST `/v1/runtimes/:id/messages` swallows a legacy kickoff for any birth-bearing contract (runtimes.ts:3116) — acks 201, creates NO turn — and relies on birth *settlement* to enqueue the run. `settleConversationBirthDebt` now returns `debt_retained` on every poll (backoff, `conversations.birth_attempts` climbs to 60+, `birth_next_attempt_at` in future, 0 runs ever). Then:
- **Connect "reading forever"** = birth debt pending → legacy GET (runtimes.ts ~2898) projects ONE synthetic queued user row → client `working=true` → `reading` never clears (client formula: `messages.length===0 && !error && (!detail||messagesLoading||kickoffSending||working)`).
- **GTKM "blank"** = its birth debt was cleared (`birth_due_at=null`, "already_satisfied") but no epoch-1 turn exists; the 15+15 epoch-0 pre-kernel turns are quarantined (server returns empty). Kickoff swallowed as "birth already done" → nothing renders.

**Scope:** 8 conversations across **7-8 andees** stuck (connect-subject + gtkm-read); grows with every new connect/gtkm loop since 07-25. &michael (`4DHFDJ8Uccv8nmGr8nG7Yj9nm9zS`) runtime `rt_x7HznHNTk7S6oqWqlecuJZ2C` was online/healthy throughout. Connor = `1473ef5c-bbb3-4215-88c6-8768bc775ce6` → connect conv_d-fsi86ek (0 msgs/0 runs, 64 birth attempts).

**Observability GAP:** birth-dispatch typed refusals (`!result.ok`, incl. `outcome_contract_not_found`) return `debt_retained` WITHOUT Sentry capture (only thrown errors hit `conversation.birth_dispatch`, of which there were zero). A 3-day fleet-wide birth outage fired no alert.

**Fix DEPLOYED TO PROD 2026-07-27 (release `v1.42.46`, staging→main merge `f7b7bade`, prod orchestrator run 30300839945 = all-green: TF apply + migrations 0143–0151 + api/mcp promote + prod smoke). ENG-1365 live in prod; migration 0151 pinned the registry loops + re-drove the 8 stuck births. Was PR #2260 (squash `accb658c`) to staging first.**
**(historical) Fix implemented on branch `fix/eng-1365-pin-registry-loop-skills`; migration `0151_pin_registry_loop_skills`:** (1) `createLoop` auto-pins `skill_version`/`skill_content_digest`/`skill_prompt` from the current published `loop_skills` row via a NEW non-validating reader `getPublishedLoopSkillPin` (NOT `getPublishedLoopSkill`, which validates the outcome contract and would 500 loop creation on a malformed spec); (2) migration 0151 backfills the pin on unpinned registry loops + re-drives stuck births — re-drive JOINs loop_skills on `content_digest = skill_content_digest` (mirrors the gate) so it never re-strands; (3) relaxed `validateOutcomeContract` AND `getLoopSkillOutcomeContract` to return null (not throw) for contract-free skills like connect (fixes a latent inspectMyRunLab 500); (4) Sentry capture on durably-stuck birth (`birthAttempts>=3`). Multi-agent code review (high) found 3 confirmed correctness bugs — all fixed. Ticket **ENG-1365** (Urgent, cycle 26, assigned michael). Follow-up deferred: author a connect outcome contract. Diagnosis via prod DB read (see [[project_raw_signal_zero_prod_throughput]] recipe; proxy on a free port, parse pw from `database-url-production`).

**FOLLOW-UP ENG-1373 (GTKM birth too slow) merged to staging 2026-07-27 (PR #2277, squash 67b61b81):** the 1.14.0 birth did an unbounded photo/raw-signal walk + synchronous enrichment drain (minutes on a large corpus). Fix = skill+migration only: SKILL.md birth now reads approved facts only (identity_read + loop items), defers photo walk + enrichment to the wider "What else is known?" pass; migration 0152 seeds GTKM 1.15.0, re-pins all existing GTKM loops to it, and re-births the born-empty/stuck cohort (rotate epoch + re-arm). NOT yet promoted to prod.
