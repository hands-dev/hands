---
name: project_clean_sheet_fleet_host_image
description: "Clean-sheet fungible-native fleet-host image (ENG-1449) — structural isolation replacing wipe-as-boundary; pillars, CS2 crash-reaper, status."
metadata: 
  node_type: memory
  type: project
  originSessionId: 0dad843a-5034-4393-9983-50fd0fefa31f
---

**Decision 2026-07-31 (Michael):** go FULL CLEAN-SHEET on the fleet-host image, abandon the retrofit (swaps #1/#2/#3 cancelled). ONE fungible-native image, isolation made STRUCTURAL, properties BAKED ONCE at build — replacing the detach-time directory-wipe that caused the `wipe_failed → self-quarantine` host-churn. Umbrella **ENG-1449**; branch `feat/eng-1450-ephemeral-checkout-fs` (C.J./wt4 owns the spine on Opus).

**Three pillars:**
- **A — Structural isolation** (Pillar A foundation COMPLETE, 4 tested primitives): per-checkout numeric uid pool (flock, crash-safe, 800000-800063) + shared `_seed_entry_filtered` (single source of truth, kills seed↔manifest drift) + `build_frozen_lower` (RO neutral-baseline snapshot, **option-B npm RO bind** under the overlay lowerdir) + standalone `_mount_checkout_overlay` (tmpfs upper + uid-owned dirs; mount syscall is Linux+root-only → canary-deferred, not locally testable).
- **B — Baked deterministic onboard + per-checkout key** (Charlie/wt6, CS5): catalog **generated from the pinned engine's own `models list`** at image build (never hand-authored — the probe caught phantom ids `gpt-5`/`gpt-5.4-codex`; `models list` exposes key/name/input/contextWindow but NOT api/cost, so those are honestly omitted, engine resolves them). Build gate: default ∈ catalog at TF-plan + `config patch --file --dry-run`. Model-agnostic PROVEN (mini `gpt-5.4-nano` builds identically to gpt-5.5). Per-checkout OpenAI key via no-store exchange envelope (key leaves the digest, MODEL stays baked).
- **C — Pre-attest at boot + fast in-host reset** (retires warm pool / INN-185) — last, depends on the spine being stable.

**CS2 = the one atomic, high-stakes assembly** (`/code-review high` when it lands): runner overlay-swap in `prepare()` + teardown enumerate-wipe→lazy-umount-discard + attestation dual-accept flip (`wipeManifestDigest`→`ephemeralFsContractDigest`, both fleet.ts gates ~2204/~1333 + both Zod schemas, fail-closed on neither) + main.tf env — ALL in one change (piecemeal = broken in-between: overlay mount + old enumerate-wipe teardown wipes a live mountpoint).

**CS2 crash-reaper (verified security finding, C.J. 2026-07-31):** `_run_detach` teardown runs as a subprocess of the still-alive checkout_executor, so the flock-held uid spans turn→detach correctly on the NORMAL path. GAP: if checkout_executor CRASHES mid-turn, flock releases (process died) but the overlay mount + tmpfs upper LINGER with andee A's data → a new checkout could allocate that freed uid while A's mount still exists = same-uid cross-andee access to A's runtime_root. FIX (in CS2 scope): a pre-uid-allocation reaper that discovers ownerless lingering checkout mounts, lazy-umounts+discards, frees the uid FIRST. Needs on-host canary coverage (Sam/WS4 isolation-proof) — crash-path logic, not unit-testable.

**POC COMPLETE — clean-sheet path VALIDATED END-TO-END on a real staging host (2026-07-31, clean-sheet-poc-wt4, kernel 6.17.0-1021-gcp; host decommissioned after, key shredded, 0 residual mounts, instance confirmed gone).** STEP 1 proved ISOLATION (two-checkout no-leak + lazy-umount-with-holder discards clean = wipe_failed structurally impossible). STEP 2 proved the ENGINE BOOTS + SERVES: real openclaw 2026.6.5 install → onboard gpt-5.5 (rc=0) → build-frozen-lower from the REAL onboarded home → per-checkout overlay mount → `openclaw agent -m` ran ON the overlay home, loaded the codex plugin FROM the overlay npm path, made a real gpt-5.5 call, returned POC_TURN_OK (agent_rc=0). A real model turn served on the clean-sheet per-checkout overlay.

**THREE ship-blocker bugs the POC caught — ALL passed local unit tests, ALL would have shipped broken to a destructive canary (committed on feat/eng-1450: 01bab22ae, f905255e2):** (1) build-frozen-lower boot call missing --expected-*-version argparse globals → rc=2 → set -e aborts boot. (2) npm bind into the lowerdir is INVISIBLE through the overlay merge (overlayfs doesn't cross submounts of its lower). (3) **npm MECHANISM — FINAL = COPY (option A):** bind→symlink→copy. The symlink fix ALSO failed on the real host because the real onboarded `.openclaw` is `0700`, so the per-checkout uid can't traverse to a symlink target under the shared home (the fake-home spike used permissive perms + missed it). Settled on **COPY npm into the frozen lower as a real root:root RO dir** (self-contained, no shared-home traversal, shared once via the lowerdir; disk verified 2.6G/22G-free). build-frozen-lower leaves npm a placeholder; the boot script `cp -a`s it in. NOTE for isolation checks: npm is a real COPIED dir now, NOT a bind or symlink. **Foreman lesson (honest): my earlier npm=B (bind) call was WRONG — chose B over A to dodge an unverifiable ~2.6G disk risk, but the real risk was the bind not surfacing through overlay, which I under-weighted; on-host reality settled on A. This is exactly why we booted early.** The 3 fixes fold forward into the CS2 atomic assembly; CS2 = harden-back-to-full-atomic-flip for the real template-lane canary (still needs the destructive reprovision + Michael's go — POC validated the MECHANISM, not yet the full fleet cold-boot).

**POC pivot (Michael, 2026-07-31):** "optimize for a POC of the new image" — validate the PATH before more optimization. We had many tested primitives but ZERO booted hosts (only the early overlay-primitive spike on host asa13isuvi23 passed on real 6.17 kernel). Whole team redirected: C.J. boots ONE throwaway staging host from her branch (disposable spike, NOT a fleet reprovision — approved) to prove boot→onboard→config-patch→gateway→overlay-mount→one-turn→2nd-checkout-no-leak; CS2 scope CUT to the minimal bootable path (defer dual-accept attestation polish + crash-reaper edge until the POC proves it). Charlie: finish CS5 (catalog needed for onboard) then get config-patch block onto the host; **task 83 versioned-image extraction PAUSED** (structure-before-substance). Sam: wire the WS4 harness to RUN against the live POC host (observe boot-verify + isolation-proof). Lesson worth keeping: don't build the full edifice before a single real host boots.

**First staging rollout — DECIDED (Michael, 2026-07-31, informed of the risk):** reprovision the WHOLE staging fungible pool onto the clean-sheet image ALL-AT-ONCE (NOT a one-host canary — fungible hosts have no dedicated agent for his &tag, so only a whole-pool roll reliably lands his chat on clean-sheet for a real test). Foreman surfaced the risk (the unattended template-lane cold-boot has never run end-to-end — "green TF plan ≠ boots"; all-at-once with no old-host fallback means a fleet-boot wedge = all staging chat down) + recommended canary-one-then-widen; Michael chose all-at-once (staging, reversible via image_version revert). Sequence: CS2 assembled → /code-review high → merge → api-server deploy (attestation gates) → whole-pool destructive reprovision (force_terraform + APPLY_DESTRUCTIVE_STAGING_PLAN). GATE: staging-deploy-orchestrator is DISABLED (todo #25) → blocks ALL staging deploys incl. CS2's api deploy → must re-enable. Re-enabling reopens the Kevin/codex clobber window. **Michael DECIDED (2026-07-31, reversing his earlier ENG-1441 decline): land Josh's ENG-1441 ref-gate FIRST** (restrict destructive force_terraform dispatch to staging/main branches in .github/scripts/guard-terraform-plan.mjs → feature-branch clobber structurally impossible), THEN re-enable. Ref-gate build delegated to Toby (Josh offline); CI-script change, mergeable in parallel with CS2, /code-review HIGH before merge. **THE DISPATCH (C.J., all 3 flags required):** `gh workflow run staging-deploy-orchestrator.yml --ref staging -f force_terraform=true -f terraform_destroy_intent=APPLY_DESTRUCTIVE_STAGING_PLAN -f force_api=true` — ⚠️ force_api=true is NON-OPTIONAL (runbook L193: manual dispatch skips app builds without it → api stays on OLD attestation gates → new hosts emit ephemeralFsContractDigest → old api rejects → every new host quarantines). Whole-pool is automatic (destructive template replace → reconcile drains version-stale + surges new from FLEET_HOST_DESIRED_IMAGE_VERSION). Confirm api-deploys-before-reconcile-surge ordering (else brief quarantine race; dual-accept + re-attest heartbeat self-heals). Foreman gives Michael a final go-heads-up before the destructive dispatch. boundAndeeId per-tag pin is MOOT for the whole-pool roll (kept for future targeted canaries).

**Rollout:** cold-boot TEMPLATE lane (destructive staging reprovision, `force_terraform=true` + `terraform_destroy_intent=APPLY_DESTRUCTIVE_STAGING_PLAN`); canary-one-host-boot-verify (green TF plan ≠ boots) → real turn → 2-sequential-checkout isolation proof → widen. **STAGING-ONLY right now** (Michael 2026-07-31) — prod parity is a deliberate later phase; the destructive canary reprovision is Michael's explicit go. Boot-observability (Phase D, #2383 merged) makes canary boot a Cloud-Logging query. Prod `FLEET_HOST_DESIRED_IMAGE_VERSION` gap (WS5 #2382) PARKED as prod work.

**Versioned host-image lane — DECIDED Shape B (Michael, 2026-07-31, task 82/83):** the fleet-host "image" is NOT an artifact (stock Ubuntu + TF-derived startup blob; identity = bootstrap_contract_digest → image_version) → versioning is a Terraform naming/STATE concern, not artifact promotion. Shape B = versioned within apps/infra: extract `modules/fleet-host-image` + instantiate `for_each` over a version set so templates COEXIST (kills the single-clobbered-`[0]` limit). Deferred Shape A (separate app) until an image app cleanly owns a {version,digest} contract. Leverage: count→for_each also sidesteps the destructive-plan guard (retained templates) + matches CS2's dual-accept N=2. Targeting (follow-on): per-cohort desired-version map + nullable `cohort` col on fleet_hosts (region-partitioned, NOT host-id — churns on recycle) + per-cohort reconcile each with its own template + fix WS5 prod-version-enforcement gap. Step 1 = no-op extract + `moved{}` migrate `fungible_host[0]`→`["key"]` in BOTH backends — **the state-mv apply is GATED on foreman review of a verified no-op plan** (wrong = destroy live template + MIG churn); must be sequenced UNDER C.J.'s CS2 (same main.tf region, CS2 is critical path).

Related: [[architecture_agent_runtimes_model]], [[feedback_foreman_cost_aware_mode]], [[project_fleet_host_bootlog_ops_agent_gap]].
