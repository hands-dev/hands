node_modules/
# compiled TS output (the package builds it); plugin/dist bundles ARE committed
agent-bus/dist/
*.log
# runtime message data — never commit (plaintext cross-worktree history)
*.db
*.db-shm
*.db-wal
*.notify
*.last-compact
*.last-utilization
