/** `agent-bus init` — one-command setup. Implementation lands with the installer. */
export async function runInit(_argv: string[]): Promise<void> {
  process.stderr.write("agent-bus init: not implemented yet (installer commit pending)\n");
  process.exit(1);
}
